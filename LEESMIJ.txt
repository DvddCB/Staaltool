App Auth V14 - Admin rol fix

Fix:
- Rol uit Supabase wordt altijd omgezet naar kleine letters.
- Admin / admin / ADMIN werken allemaal.
- isAdmin check gebruikt nu genormaliseerde rol.
- Als profile ontbreekt, toont de app de UID die in profiles moet staan.

Belangrijk:
Als Daan opnieuw is aangemaakt in Supabase, heeft hij een nieuwe UID.
Controleer:
1. Supabase -> Authentication -> Users -> Daan -> kopieer UID
2. Supabase -> Table Editor -> profiles
3. De id in profiles moet exact die UID zijn
4. role mag Admin of admin zijn

Gebruik:
1. Vervang src/App.jsx
2. Commit changes
3. Wacht op Vercel Ready
4. Log uit/in of Ctrl + F5
